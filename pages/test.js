/*
import SuccesNotification,{succes,warning} from '../component/notification/Notification'
import {useState} from "react";
/!*import ReactNotification from 'react-notifications-component'
import { store } from 'react-notifications-component';
const a=()=>{
    store.addNotification({
        title: "Wonderful!",
        message: "teodosii@react-notifications-component",
        type: "success",
        insert: "top",
        container: "top-right",
        animationIn: ["animated", "fadeIn"],
        animationOut: ["animated", "fadeOut"],
        dismiss: {
            duration: 5000,
            onScreen: true
        }
    })}*!/
function test() {
    const [error,setError]=useState(false)
    const Lol=()=>{
        warning({title:"alireza",message:"aliz"})
    }
    return(
        <div>
            <SuccesNotification/>
            {error===true && <Lol/>}
            <button onClick={()=>Lol()}>A</button>
        </div>
    )
}
export default test*/
